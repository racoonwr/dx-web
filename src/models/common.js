import { Effect } from "umi";
// import { getUrlMap } from "../services/tool";

const Model = {
  namespace: "common",
  state: {},
  effects: {
    /* 分享链接 */
    // * getUrlMap({ payload }, { call }) {
    //   return yield call(getUrlMap, payload);
    // }
  },
  reducers: {},
};

export default Model;
